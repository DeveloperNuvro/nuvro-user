# Backend API base (no trailing slash). Required when frontend and API are on different origins — needed for WhatsApp voice / Unipile audio playback and media proxy.
VITE_API_BASE_URL=http://localhost:7575
